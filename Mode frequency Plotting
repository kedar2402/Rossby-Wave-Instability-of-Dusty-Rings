


import matplotlib.pyplot as plt
import numpy as np


# make data
x=np.array([0,0.010,.....0.10])
y=np.array([ Mode frequency values])

# plot


ax.plot(x, p, linewidth=2.0)
ax.set(xlim=(0, 2), xticks=np.arange(0,2, 0.2))
plt.plot(x,y)
plt.grid()
plt.title(" Model frequency vs Amplitude model")
plt.ylabel("amplitude")
plt.xlabel("mode frequency")


plt.show()
